{
    "name": "beefsteak-map-tiles-demo",
    "type": "module",
    "version": "0.0.1",
    "scripts": {
        "build": "rm -rf dist/ && mkdir -p dist && cp -a node_modules/maplibre-gl/dist/. dist/maplibre/ && npm run sprites",
        "serve": "node ./serve.js",
        "sprites": "spreet --unique --minify-index-file style/sprites/svg style/sprites/demo && spreet --retina --unique --minify-index-file style/sprites/svg style/sprites/demo@2x"
    },
    "devDependencies": {
        "maplibre-gl": "~5.17.0"
    }
}
